/**
 * Production environment defaults. Overridden in development via the
 * fileReplacements entry in angular.json (see environment.development.ts).
 */
export const environment = {
  production: true,
  apiBaseUrl: '/api',
};
